import 'jest';
import Vue from 'vue';
import ResetPassword from '../src/components/reset-password';

describe('ResetPassword', () => {
  it('1 plus 1 should be 2', () => {
    expect(1 + 1).toBe(2);
  });

  it('clear should call clear on form', () => {
    // add test logic here
  });
});
